\documentclass[border=4pt]{standalone}
\usepackage{mathpazo}
\usepackage{xcolor}
\usepackage{tikz}
\usetikzlibrary{positioning,arrows.meta,fit,calc}
\definecolor{accent}{RGB}{96,96,32}
\begin{document}
\begin{tikzpicture}[
  src/.style={draw=accent, rounded corners=2pt, align=center, font=\scriptsize,
              inner sep=5pt, text width=34mm, minimum height=13mm},
  res/.style={draw=accent, thick, fill=accent!14, rounded corners=2pt, align=center,
              font=\scriptsize, inner sep=5pt, text width=34mm, minimum height=15mm},
  arr/.style={-{Stealth[length=2.2mm]}, accent, thick},
  mid/.style={font=\tiny\itshape, text=accent, align=center, inner sep=2pt}]

\node[src] (a1) {\textbf{Reduced-order surrogate}\\[-1pt]{\tiny one fixed basis}};
\node[src, right=13mm of a1] (b1) {\textbf{Pretrained or}\\\textbf{fine-tuned transformer}\\[-1pt]{\tiny weights frozen, no gradients}};
\node[src, right=13mm of b1] (c1) {\textbf{A stochastic model to tune}\\[-1pt]{\tiny every evaluation is noisy}};

\node[res, below=17mm of a1] (a2) {\textbf{Model error you can propagate}\\[-1pt]{\tiny SS-PPCA · SS-Bootstrap}};
\node[res, below=17mm of b1] (b2) {\textbf{Calibrated forecasts, backbone untouched}\\[-1pt]{\tiny stochastic attention}};
\node[res, below=17mm of c1] (c2) {\textbf{Same answer, $40\times$ fewer runs}\\[-1pt]{\tiny BO under uncertainty}};

\draw[arr] (a1) -- node[mid, right]{a \emph{distribution}\\ over subspaces} (a2);
\draw[arr] (b1) -- node[mid, right]{resample attention\\ at inference} (b2);
\draw[arr] (c1) -- node[mid, right]{make the noise\\ part of the objective} (c2);

\node[mid, above=3mm of b1, text width=95mm, font=\scriptsize\itshape]
  {Put the uncertainty in the model's own structure --- then calibrate it, then let it decide.};
\end{tikzpicture}
\end{document}
